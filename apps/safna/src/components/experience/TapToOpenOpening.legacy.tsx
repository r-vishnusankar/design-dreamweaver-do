"use client";

/**
 * Legacy opening (v1) — superseded by the gate-fold CardUnfoldOpening.
 * Kept as an alias so FEATURES.useLegacyOpening still resolves.
 */
export { CardUnfoldOpening as TapToOpenOpeningLegacy } from "./CardUnfoldOpening";
